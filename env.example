# =============================================================================
# MLflow / DagsHub Configuration
# =============================================================================
# Required: MLflow tracking URI (DagsHub or local MLflow server)
# For DagsHub: https://dagshub.com/<owner>/<repo>.mlflow
# For local MLflow: http://localhost:5000
MLFLOW_TRACKING_URI=https://dagshub.com/YOUR_USERNAME/YOUR_REPO.mlflow

# Optional: MLflow experiment name (default: "churn_prediction")
MLFLOW_EXPERIMENT_NAME=churn_prediction

# Optional: DagsHub authentication token (if using DagsHub)
# Get your token from: https://dagshub.com/<owner>/<repo>/settings/tokens
DAGSHUB_TOKEN=your_dagshub_token_here

# Optional: DagsHub repo metadata (for documentation)
DAGSHUB_REPO_OWNER=YOUR_USERNAME
DAGSHUB_REPO_NAME=YOUR_REPO

# =============================================================================
# Training Configuration
# =============================================================================
# Path to training data CSV file
DATA_PATH=data/churn.csv

# Name of the target column in the CSV
TARGET_COLUMN=churn

# Hyperparameter tuning (optional)
# Set to "1", "true", or "yes" to enable RandomizedSearchCV hyperparameter tuning
# This will test 30 parameter combinations with 3-fold CV (takes longer but may improve performance)
ENABLE_HYPERPARAMETER_TUNING=false

# =============================================================================
# Feast Feature Store Configuration (Optional)
# =============================================================================
# Set to "1", "true", or "yes" to use Feast for feature loading
# If not set or empty, will use raw CSV data directly
USE_FEAST=0

# Feast feature view name (required if USE_FEAST=1)
FEAST_FEATURE_VIEW=churn_features

# Entity column name for Feast joins (required if USE_FEAST=1)
FEAST_ENTITY_COLUMN=customer_id

# =============================================================================
# Model Registry Configuration (for Inference)
# =============================================================================
# Model name in MLflow Model Registry (default: "ChurnModel")
MLFLOW_MODEL_NAME=ChurnModel

# Model stage to load for inference (default: "Production")
# Options: "Staging", "Production", "Archived", or "None"
MLFLOW_MODEL_STAGE=Production

# =============================================================================
# AWS Configuration (for Lambda Deployment - Optional for local testing)
# =============================================================================
# AWS region for ECR and Lambda
AWS_REGION=us-east-1

# ECR repository name for Docker images
ECR_REPOSITORY=churn-prediction-lambda

# AWS IAM Role ARN for Lambda execution (typically set in GitHub Secrets)
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/lambda-execution-role

# =============================================================================
# Local Development / Testing
# =============================================================================
# Optional: Override default paths for local testing
# PYTHONPATH=.
# LOG_LEVEL=INFO

